% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{gene_boxViolin_plot}
\alias{gene_boxViolin_plot}
\alias{gene_boxViolin_plot,omicscope-method}
\title{Create Box or Violin Plots for Selected Genes}
\usage{
gene_boxViolin_plot(object, ...)

\S4method{gene_boxViolin_plot}{omicscope}(
  object,
  selected_gene = NULL,
  fill_var = "group",
  fill_col = c("grey", "red"),
  type = c("box", "violin"),
  box_width = 0.75,
  error_bar_width = 0.25,
  add_point = TRUE,
  jitter.width = 0.3,
  point_col = "grey30",
  point_alpha = 0.8
)
}
\arguments{
\item{object}{An object of class \code{omicscope} containing normalized
expression data and sample metadata.}

\item{...}{Additional arguments (currently not used).}

\item{selected_gene}{Character vector specifying the gene names to plot.
Required parameter. Gene names should match those in the normalized data.}

\item{fill_var}{Character string specifying the column name in the metadata
to use for grouping and coloring. Default is \code{"group"}.}

\item{fill_col}{Character vector of colors to use for filling the groups.
Default is \code{c("grey", "red")}.}

\item{type}{Character string specifying the plot type. Options are \code{"box"}
for box plots or \code{"violin"} for violin plots. Default is \code{"box"}.}

\item{box_width}{Numeric value specifying the width of boxes in box plots.
Default is \code{0.75}. Only applicable when \code{type = "box"}.}

\item{error_bar_width}{Numeric value specifying the width of error bars in
box plots. Default is \code{0.25}. Only applicable when \code{type = "box"}.}

\item{add_point}{Logical value indicating whether to add individual data points
to the plot. When \code{TRUE}, points are displayed using jitter to avoid
overplotting. Default is \code{TRUE}.}

\item{jitter.width}{Numeric value controlling the amount of horizontal jitter
for individual points. Higher values increase the spread of points.
Default is \code{0.3}. Only used when \code{add_point = TRUE}.}

\item{point_col}{Character string specifying the color of individual data points.
Default is \code{"grey30"}. Only used when \code{add_point = TRUE}.}

\item{point_alpha}{Numeric value between 0 and 1 controlling the transparency
of individual data points. Lower values make points more transparent.
Default is \code{0.8}. Only used when \code{add_point = TRUE}.}
}
\value{
A \code{ggplot2} object displaying the gene expression plot with
statistical comparisons.
}
\description{
This function generates box plots or violin plots to visualize the expression
levels of selected genes across different groups (e.g., Normal vs Tumor).
Statistical comparisons between groups are performed using the Wilcoxon test.
}
\details{
The function performs the following steps:
\itemize{
\item Extracts normalized expression data from the \code{omicscope} object
\item Filters data for the specified genes
\item Transforms expression values using log2(value + 1)
\item Creates either box plots or violin plots based on the \code{type} parameter
\item Adds statistical comparisons using Wilcoxon test (p-values shown as significance symbols)
}

The function requires that normalized data has been generated first using the
\code{get_normalized_data} function.
}
\examples{
\dontrun{
# Create box plots for selected genes
gene_boxViolin_plot(object = obj,
                    selected_gene = c("METTL5", "METTL16", "METTL3",
                                      "METTL14", "ACTB", "GAPDH"))

# Create violin plots
gene_boxViolin_plot(object = obj,
                    selected_gene = c("METTL5", "METTL16", "METTL3",
                                      "METTL14", "ACTB", "GAPDH"),
                    type = "violin")

# Customize colors and box width
gene_boxViolin_plot(object = obj,
                    selected_gene = c("ACTB", "GAPDH"),
                    fill_col = c("#0073C2FF", "#EFC000FF"),
                    box_width = 0.5)
}


}
