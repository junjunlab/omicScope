% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{get_normalized_data}
\alias{get_normalized_data}
\alias{get_normalized_data,omicscope-method}
\title{Extract and format normalized expression data}
\usage{
get_normalized_data(object, ...)

\S4method{get_normalized_data}{omicscope}(object)
}
\arguments{
\item{object}{An \code{omicscope} object containing normalized expression data.
The object must have been processed with \code{\link{normalize_data}} first.}

\item{...}{Additional arguments (currently not used)}
}
\value{
An \code{omicscope} object with updated \code{normalizedData} slot
}
\description{
This function extracts normalized expression data from an \code{omicscope}
object and formats it into both wide and long formats for downstream analysis
and visualization.
}
\examples{
\dontrun{
# First normalize the data
omics_obj <- normalize_data(omics_obj, norm_type = "tpm")

# Then extract formatted normalized data
omics_obj <- get_normalized_data(omics_obj)

# Access data
wide_data <- omics_obj@normalizedData


}

}
\seealso{
\code{\link{normalize_data}}
}
